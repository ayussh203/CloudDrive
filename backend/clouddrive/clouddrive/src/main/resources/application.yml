server:
  port: ${PORT:5000}

spring:
  application:
    name: clouddrive
  datasource:
    url: jdbc:postgresql://clouddrive-db-public.czio2aggmdzt.ap-south-1.rds.amazonaws.com:5432/clouddrive
    username: cloudadmin
    password: CloudDriveP#ss123!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

management:
  endpoints:
    web:
      exposure:
        include: "health,info"

app:
  jwt:
    secret: "dev-change-me-please-32+chars-long"
    ttlMinutes: 60
  s3:
    bucket: "clouddrive-dev-ayush"
    region: "ap-south-1"  

  
